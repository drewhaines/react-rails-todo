<%= form_for(@mainline) do |f| %>
  <% if @mainline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mainline.errors.count, "error") %> prohibited this mainline from being saved:</h2>

      <ul>
      <% @mainline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :site_id %><br>
    <%= f.number_field :site_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
